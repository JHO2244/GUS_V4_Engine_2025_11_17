name: GUS v4 — Release Attestation (Tags Only)

on:
  push:
    tags:
      - "v*.*"
      - "v*.*.*"

jobs:
  release_attestation:
    runs-on: windows-latest
    env:
      GUS_CI: "1"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONDONTWRITEBYTECODE: "1"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Create venv + install deps
        shell: bash
        run: |
          python -m venv venv
          source venv/Scripts/activate
          python -m pip install --upgrade pip
          python -m pip install pytest cryptography

      - name: Tests (release gate)
        shell: bash
        run: |
          source venv/Scripts/activate
          pytest

      - name: Verify epoch anchor (signed) for release
        run: |
          python -m scripts.verify_epoch_anchor --manifest epochs/epoch_bada220e_20251221T120542Z/epoch_manifest_v0_1.json
      
